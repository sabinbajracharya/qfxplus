import facebookUtil from 'qfxplus/utils/facebook-util';
import { module, test } from 'qunit';

module('Unit | Utility | facebook util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = facebookUtil();
  assert.ok(result);
});
