import timeago from 'qfxplus/utils/timeago';
import { module, test } from 'qunit';

module('Unit | Utility | timeago');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeago();
  assert.ok(result);
});
