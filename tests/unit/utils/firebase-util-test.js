import firebaseUtil from 'qfxplus/utils/firebase-util';
import { module, test } from 'qunit';

module('Unit | Utility | firebase util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = firebaseUtil();
  assert.ok(result);
});
